# GasVision Dispatcher Frontend

React/Vite прототип панели диспетчера.

## Запуск

```bash
npm install
npm run dev
```

По умолчанию фронт ожидает BFF на `http://localhost:8010`.

Если нужно переопределить API:

```bash
VITE_API_BASE_URL=http://localhost:8010 npm run dev
```
